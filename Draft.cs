using System;
using System.Collections.Generic;
using System.Text;

namespace Miterya.ScreenTest
{
    class Draft
    {
        #region Checking Url Address 
        //public string expectedUrl = "http://localhost:33069/";
        //String URL = webDriver.Url;
        //Assert.AreEqual(URL, expectedUrl);
        #endregion

        #region Contains Text Body
        //IWebElement body = webDriver.FindElement(By.TagName("body"));

        //Assert.IsTrue(body.Text.Contains("Ana Sayfa"));//basic cozum optimize edilmeli
        #endregion
    }
}
